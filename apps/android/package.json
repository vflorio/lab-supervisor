{
  "name": "@supervisor/android",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "android:build": "./gradlew :app:assembleRelease",
    "android:build:debug": "./gradlew :app:assembleDebug"
  }
}
