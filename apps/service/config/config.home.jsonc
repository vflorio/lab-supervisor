{
  "suitest": {
    "baseUrl": "http://localhost:3002/v1/suitest",
    "tokenId": "dev-suitest-token-id",
    "tokenPassword": "dev-suitest-token-password"
  },
  "slack": {
    "active": false,
    "botToken": "dev-slack-token"
  },
  "trpc": {
    "port": 3001,
    "hostname": "127.0.0.1"
  },
  "tracking": {
    "adb": { "polling": [["constantDelay", "10s"]] }, // poll locale, nessun costo esterno
    "suitestCamera": { "polling": [["constantDelay", "30s"]] },
    "suitestControlUnit": { "polling": [["constantDelay", "60s"]] },
    "suitestDevice": { "polling": [["constantDelay", "60s"]] }
  },
  "log": {
    "level": "debug", // "error" | "warn" | "info" | "debug"
    "path": "data/logs.jsonl", // JSON Lines files
    "network": false // stampa le risposte HTTP (get/post, singole o paginate) indipendentemente da "tripwire"
  },
  "adb": {
    "port": 5555,
    // Pazienza di waitForDevice: quanto un workflow attende la riconnessione (già in corso in
    // background) prima di arrendersi. La forma dei singoli tentativi non è configurabile.
    "waitForDeviceTimeout": "120s"
  },
  "activationSchedule": {
    "days": ["monday", "tuesday", "wednesday", "thursday", "friday", "saturday", "sunday"],
    "from": "00:00",
    "to": "23:59"
  },
  // Escalation sequenziale
  "workflows": [
    [
      "open-chrome",
      [
        // wake screen up
        ["wakeUp"],
        // Ensure App is running (start if not opened)
        ["ensureActivity", "com.android.chrome", "com.google.android.apps.chrome.Main"]
      ]
    ],
    [
      "open-chrome-reboot",
      [
        // Assicurarsi che reboot non sia mai al primo livello per evitare loops dato che waitForDevice è fallato
        ["reboot"],
        // waitForDevice attende ora l'AndroidBridge (mDNS + re-pairing), non la porta 5555 congelata
        ["waitForDevice"],
        ["sleep", "5s"],
        ["ensureActivity", "com.android.chrome", "com.google.android.apps.chrome.Main"]
      ]
    ],
    ["open-developer-settings", [["wakeUp"], ["openDeveloperSettings"]]]
  ],
  "recovery": [
    {
      "domain": "suitest-camera",
      "label": "camera-recovery-demo",
      // Tripwire indipendente
      "tripwires": [
        {
          // Quanto tempo il predicate deve rimanere falso prima di scattare la pipeline
          "grace": "10s",
          // Cosa osservare
          "predicate": ["ref", "suitest_camera_connected"],
          // Cosa fare quando il predicate resta falso per >= grace
          "pipeline": ["workflow", "open-chrome-reboot"],
          // Cosa fare se la pipeline fallisce (es. crash, timeout, ecc.): il delay deve superare
          // sia adb.waitForDeviceTimeout (120s, il tempo massimo che la pipeline può restare
          // "in attesa" prima di fallire) sia tracking.suitestCamera.polling (30s, il tempo che
          // Suitest impiega a ripollare e aggiornare il predicate) - altrimenti si ritenta
          // (quindi si reboota di nuovo) un device già tornato sano ma non ancora ripollato
          "retry": [["constantDelay", "120s"], ["limitRetries", 2]],
          "notify": [
            {
              "type": ["slack"],
              "channel": "#lab-supervisor",
              "message": "{{label}} ({{ip}}) suitest connection failed, manual intervention required",
              "policy": ["exhausted"]
            }
          ]
        }
      ]
    }
  ],
  "registry": {
    "dbPath": "data/device-registry.json",
    "devices": {
      "cameras": [
        {
          "id": "tablet",
          "label": "Lenovo Tab M10",
          "controlled": true,
          "videoCaptureDeviceId": null,
          "adbId": "192.168.1.4:5555"
        }
      ],
      "tvs": [{ "deviceId": "tv-001", "label": "TV_1", "controlled": false, "ip": "192.168.2.1" }],
      "adb": [{ "id": "192.168.1.4:5555", "label": "Lenovo Tab M10", "target": "192.168.1.4:5555" }]
    }
  }
}
